import { PriorityColor } from './priority-color';

describe('PriorityColor', () => {
  it('should create an instance', () => {
    const directive = new PriorityColor();
    expect(directive).toBeTruthy();
  });
});
